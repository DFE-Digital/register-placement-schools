apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: development.register-of-placement-schools.education.gov.uk
spec:
  ingressClassName: nginx
  rules:
  - host: development.register-of-placement-schools.education.gov.uk
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: register-of-placement-schools-maintenance
            port:
              number: 80
