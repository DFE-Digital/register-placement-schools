apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: register-placement-schools-development.test.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
    - host: register-placement-schools-development.test.teacherservices.cloud
      http:
        paths:
          - pathType: ImplementationSpecific
            backend:
              service:
                name: register-placement-schools-maintenance
                port:
                  number: 80
