apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: register-of-placement-schools-development.test.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: register-of-placement-schools-development.test.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: register-of-placement-schools-development
            port:
              number: 80
