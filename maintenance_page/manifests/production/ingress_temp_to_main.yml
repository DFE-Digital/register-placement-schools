apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: register-placement-schools-temp.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
    - host: register-placement-schools-temp.teacherservices.cloud
      http:
        paths:
          - pathType: ImplementationSpecific
            backend:
              service:
                name: register-placement-schools-production
                port:
                  number: 80
