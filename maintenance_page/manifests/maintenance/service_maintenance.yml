---
apiVersion: v1
kind: Service
metadata:
  name: register-placement-schools-maintenance
spec:
  selector:
    app: register-placement-schools-maintenance
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
