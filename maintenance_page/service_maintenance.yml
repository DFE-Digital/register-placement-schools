---
apiVersion: v1
kind: Service
metadata:
  name: register-of-placement-schools-maintenance
spec:
  selector:
    app: register-of-placement-schools-maintenance
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
